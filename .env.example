# Generate a unique value with:
# python -c "import secrets; print(secrets.token_urlsafe(48))"
PROMPT_LAB_AUTH_SECRET=

# Optional live model providers. The deterministic mock provider works without keys.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
